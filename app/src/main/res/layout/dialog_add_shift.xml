<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="הוספת משמרת חדשה"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"/>

    <EditText
        android:id="@+id/etDialogStartTime"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="שעת התחלה (לחץ לבחירה)"
        android:focusable="false"
        android:inputType="none"
        android:layout_marginBottom="12dp"/>

    <EditText
        android:id="@+id/etDialogEndTime"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="שעת סיום (לחץ לבחירה)"
        android:focusable="false"
        android:inputType="none"
        android:layout_marginBottom="12dp"/>

    <EditText
        android:id="@+id/etDialogRequiredWorkers"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="כמות עובדים נדרשת (לדוגמה: 3)"
        android:inputType="number" />

</LinearLayout>